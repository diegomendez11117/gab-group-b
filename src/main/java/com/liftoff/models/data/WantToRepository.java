package com.liftoff.models.data;

import com.liftoff.models.WantTo;
import org.springframework.data.jpa.repository.JpaRepository;

public interface WantToRepository extends JpaRepository<WantTo, Integer> {
}
